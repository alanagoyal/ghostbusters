# ============================================================================
# Environment Configuration for Doorstep Costume Classifier
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real credentials!
#
# Note: This single .env file is used by both:
#   - Backend (Python) - runs on Raspberry Pi
#   - Frontend (Next.js) - runs in dashboard/ directory
#
# The frontend automatically reads variables from parent directory's .env file

# ============================================================================
# BACKEND CONFIGURATION (Raspberry Pi)
# ============================================================================

# --- DoorBird Camera ---
# Get these from your DoorBird's web interface
# API user must have "API-Operator" and "Live Video" permissions
DOORBIRD_IP=192.168.x.x
DOORBIRD_USERNAME=your_doorbird_api_username
DOORBIRD_PASSWORD=your_doorbird_api_password

# --- Supabase (Backend) ---
# Use SERVICE_ROLE_KEY for backend - it has full database access
# Get from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# --- Device Identification ---
# Identifies this Raspberry Pi in multi-device setups
# Used for organizing images in storage (e.g., "front-door-pi/image.jpg")
HOSTNAME=halloween-pi

# --- Baseten API (Optional - Future Feature) ---
# For AI costume classification using vision-language models
# BASETEN_API_KEY=your_baseten_api_key

# ============================================================================
# FRONTEND CONFIGURATION (Next.js Dashboard)
# ============================================================================

# --- Supabase (Frontend) ---
# Use ANON_KEY for frontend - it's restricted by Row Level Security (RLS)
# Get from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================================================
# NOTES
# ============================================================================
#
# Backend uses:
#   - DOORBIRD_* (camera access)
#   - NEXT_PUBLIC_SUPABASE_URL (database connection)
#   - SUPABASE_SERVICE_ROLE_KEY (full database access)
#   - HOSTNAME (device identification)
#
# Frontend uses:
#   - NEXT_PUBLIC_SUPABASE_URL (database connection)
#   - NEXT_PUBLIC_SUPABASE_ANON_KEY (restricted database access)
#
# Security:
#   - SERVICE_ROLE_KEY = Full access (backend only, never expose to browser)
#   - ANON_KEY = Restricted access (safe for browser, protected by RLS)
#
# Setup order:
#   1. Configure DoorBird camera (docs/setup/doorbird.md)
#   2. Create Supabase project (docs/setup/supabase.md)
#   3. Set up backend on Pi (docs/setup/backend.md)
#   4. Deploy dashboard (dashboard/SETUP.md)
